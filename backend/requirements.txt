fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# Pinned: passlib 1.7.4's bcrypt version-detection probe crashes against
# bcrypt>=4.1 (which removed the `__about__` attribute it reads) - this
# breaks password hashing entirely, not just a warning. Confirmed by
# running the real test suite; see tests/test_auth.py.
python-multipart==0.0.9
redis==5.0.8
httpx==0.27.2
python-dateutil==2.9.0.post0
slowapi==0.1.9
sentry-sdk[fastapi]==2.14.0
